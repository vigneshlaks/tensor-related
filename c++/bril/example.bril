@main {
  # Entry block
  a: int = const 5;
  b: int = const 10;
  cond: bool = lt a b;
  br cond .then .else;

  # Then block
  .then:
  result: int = add a b;
  jmp .end;

  # Else block
  .else:
  result: int = mul a b;
  jmp .end;

  # End block
  .end:
  print result;
}
